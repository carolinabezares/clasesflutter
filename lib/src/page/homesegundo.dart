import 'package:flutter/material.dart';

class HomeSegundo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Vista dos")));
  }
}
